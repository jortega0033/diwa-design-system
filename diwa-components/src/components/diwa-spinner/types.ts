/**
 * diwa-spinner — Public TypeScript interfaces
 * ============================================
 * Exported so framework wrapper packages and consumers can share these types.
 */

/** Size tier for the spinner. */
export type SpinnerSize = "sm" | "md" | "lg";
